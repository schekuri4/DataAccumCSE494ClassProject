# Archive

This folder is for legacy material that is useful to keep nearby but should not clutter the active project layout.

Tracked notes can live here. Bulky logs, scratch experiments, generated compiler output, and one-off helpers should go under `archive/local/`, which is intentionally ignored by Git.
